{
  "devDependencies": {
    "@sonar/scan": "^4.3.4"
  },
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@heroapp/shared && npm run build --workspace=@heroapp/apps/Hero-Collector-App && npm run build --workspace=@heroapp/Backend"
  }
}
