{
  "devDependencies": {
    "@sonar/scan": "^4.3.4",
    "@types/jest": "^30.0.0",
    "@types/supertest": "^6.0.3",
    "jest": "^30.5.0",
    "supertest": "^7.2.2"
  },
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "start": "npm run start --workspace=apps/Backend",
    "test": "jest"
  },
  "type": "module",
  "dependencies": {
    "ts-jest": "^29.4.12"
  }
}
