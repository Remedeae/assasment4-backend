{
  "devDependencies": {
    "@sonar/scan": "^4.3.4"
  },
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "start": "npm run start --workspace=apps/Backend"
  }
}
