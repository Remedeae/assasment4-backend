import { Router } from "express";

const router = Router();

//post rewarded hero after game by ID
router.post("/id", async (req, res, next) => {
  try {
  } catch (error) {
    next(error);
  }
});

export default router;
